<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Se o diretório ou arquivo existir, use-o diretamente
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  # Caso contrário, encaminhe para o index.html
  RewriteRule ^ index.html [L]
</IfModule>